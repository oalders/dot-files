1) dot files        # cd ~/dot-files && ./install.sh
2) git gc           # git prune && git lfs prune && git gc
3) cpm install      # cpm install -g --cpanfile cpanfile
4) parallel prove   # prove -lr -j $MY_PROCS t
