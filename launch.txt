1) install Perl deps   # cpm install -g --cpanfile cpanfile
2) git gc              # git prune && git lfs prune && git gc
3) install dot files   # cd ~/dot-files && bash install.sh
4) prove in parallel   # prove -lr -j $MY_PROCS t
