{
  "meta": {
    "name": "oalders-ansible",
    "description": "Ansible CLI mixin: read the pipx-installed ansible venv (so its ansible-vault / ansible-playbook binaries run) plus user-installed Galaxy collections under ~/.ansible/collections. Filesystem only; no network egress."
  },
  "filesystem": {
    "read": [
      "~/.local/share/pipx/venvs/ansible",
      "~/.ansible/collections"
    ]
  }
}
