---
extends: default

rules:
  indentation:
    spaces: 2
  # 80 chars should be enough, but don't fail if a line is longer
  line-length:
    max: 80
    level: warning
  truthy:
    allowed-values: ["true", "false", "on"]
